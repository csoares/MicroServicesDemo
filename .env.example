# Copy this file to .env and adjust values for your environment.
# docker-compose.yml reads these values if you use variable substitution.
# For the classroom demo the values are hardcoded in docker-compose.yml for simplicity.

POSTGRES_USER=demo
POSTGRES_PASSWORD=demo123

JWT_SECRET=supersecretkey-change-in-production

RABBITMQ_DEFAULT_USER=guest
RABBITMQ_DEFAULT_PASS=guest
